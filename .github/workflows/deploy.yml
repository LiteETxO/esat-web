# GitHub Pages deploy disabled — app requires Node.js SSR (Vercel).
# TODO: replace this file with a Vercel deploy workflow once project is configured.
# Preview URL: http://161.35.220.149:3002/
name: Deploy (disabled)
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy to Vercel — not yet configured. See README."
